# A single service and nothing else: should deploy straight away with no
# database provisioning and no waiting.
services:
  app:
    build: .
    ports:
      - "4000:4000"
    environment:
      NODE_ENV: production
