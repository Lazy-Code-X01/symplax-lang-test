{
  "name": "docker-node-prisma-alpine",
  "version": "1.0.0",
  "description": "Node + Express + Prisma on node:alpine — tests the musl/openssl auto-patch (the 'sub' scenario)",
  "scripts": {
    "build": "prisma generate",
    "start": "node index.js"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "prisma": "^5.22.0"
  }
}
