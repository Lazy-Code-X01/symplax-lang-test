<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <title>Laravel on Symplax</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <style>
        body {
            background: #0a0f0d;
            color: #f0fdf4;
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
            display: flex;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            margin: 0;
        }
        .card {
            text-align: center;
            padding: 3rem;
        }
        h1 {
            color: #10b981;
            font-size: 2.5rem;
            margin-bottom: 0.5rem;
        }
        p {
            color: #9ca3af;
            font-size: 1.1rem;
        }
        code {
            background: #152019;
            padding: 0.3rem 0.6rem;
            border-radius: 6px;
            color: #10b981;
        }
        .badge {
            display: inline-block;
            background: #152019;
            border: 1px solid #10b981;
            color: #10b981;
            padding: 0.4rem 1rem;
            border-radius: 20px;
            font-size: 0.85rem;
            margin-top: 1.5rem;
        }
    </style>
</head>
<body>
    <div class="card">
        <h1>🚀 Laravel on Symplax</h1>
        <p>This app was deployed via Symplax — zero terminal required.</p>
        <p>Try <code>/api/health</code> or <code>/api/test</code></p>
        <div class="badge">Deployed with Symplax</div>
    </div>
</body>
</html>
